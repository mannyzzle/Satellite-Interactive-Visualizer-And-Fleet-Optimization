[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    live: tests that hit the live Railway API (read-only). Skip with `-m "not live"`.
    load: tests that drive sustained load. Run manually only.
